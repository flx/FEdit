# TODO

Open work. Each item: `- [ ] (slug) Title — optional notes`.
The `(slug)` is how you reference the item in `/plan`, `/implement`, `/done`.
Add an explicit slug so it stays stable even if you reword the title.

Items are in ship order; each depends only on items above it (dependencies
called out per item). Spec references are to SPEC.md sections. Notes name the
files an item touches so overlapping items don't get batched in parallel.

## Features

- [ ] (baked-in-autosave) Always-on autosave (baked in) — replace open-save's opt-in "Autosave on File Switch" model with UNCONDITIONAL autosave: after the user stops typing, write the open file to disk on a short debounce (~0.5–1 s), and also on file switch / focus loss / window close / quit. Goal: the on-disk file tracks the editor quickly so an external tool (e.g. Claude editing the same file) has the smallest possible window to clobber unsaved edits, and "dirty" becomes transient. `Models/WorkspaceModel.swift` (a debounced autosave hung off the `editorText` setter, reusing `saveOpenFile()`; failed-save still surfaced), driven from `Editor/CodeEditorView.swift`/`Views/ContentView.swift`; `App/FEditApp.swift` — the "Autosave on File Switch" toggle and the four-button unsaved-changes NSAlert from (open-save) become largely moot: decide whether to remove them or keep as an override. MUST coordinate with (external-change-watch): a pending autosave must not fight a just-detected external reload (last-writer race). Interacts heavily with (open-save)'s dirty/save/dialog model. Update SPEC §7, §10. Depends on (open-save).
- [ ] (external-change-watch) Watch open file(s) for external changes and refresh the view — today FEdit has NO file-system watcher (SPEC §5.2 "refresh is manual"), so a file changed on disk (e.g. by Claude) is not reflected until you switch files. Add a `DispatchSource` (vnode) / FSEvents watcher of the currently open file (and ideally the sidebar tree roots); when the file changes on disk and the editor buffer is clean (matches last-saved), reload it into the editor preserving cursor/scroll; when the buffer has unsaved edits, apply a conflict policy (with (baked-in-autosave) the dirty window is tiny — default: the in-editor version wins, no silent clobber, optionally a subtle "changed on disk" indicator). Also refresh the sidebar tree on external add/remove (currently manual Refresh only). New `Models/FileWatcher.swift` (a `DispatchSourceFileSystemObject` wrapper handling atomic-save rename via re-arm), wired into `Models/WorkspaceModel.swift`'s load path and the folder scan. Coordinate with (baked-in-autosave). Update SPEC §5.2. Depends on (open-save), (folder-sidebar).
- [ ] (git-changed-badge) Git "(changed)" badge in the file list — when an opened top-level folder is the ROOT of a git repository (contains `.git`), show a small right-aligned "(changed)" badge on each FILE row (NOT directories) whose content differs from the last commit: `|foo.md          |` unchanged, `|foo.md  (changed)|` changed. Compute via shelling out to `git` (app is unsandboxed, SPEC §2) — `git status --porcelain`/`git diff --name-only HEAD` plus untracked — for the repo root, mapped to root-relative paths; cache and refresh on save, on (external-change-watch) events, on manual Refresh, and when HEAD moves. New `Models/GitStatus.swift` (repo-root detection, `Process`-based git invocation off the main thread, parse, map to file URLs); `Views/SidebarView.swift` `FileRow` gains the trailing badge (files only; also in the flat filtered list). Directories never badged. Depends on (folder-sidebar). Spec: add a new "file status" section.

## Bugs

- [ ] (memory-use-audit) Investigate steady-state memory use (80–190 MB with only tiny files open) — profile with Instruments (Allocations + Leaks), don't guess. GOAL is GENUINE reductions only: the user explicitly rejects lazy-offload-then-reload tradeoffs (e.g. do NOT unload the markdown preview/highlighter and reload on demand). Prime suspects, worst-first: (1) **Leak hunt** — does a CLOSED window fully release its per-window `WorkspaceModel` + BOTH TextKit 1 stacks (editor `CodeEditorView` + markdown `MarkdownPreviewView`, each NSTextStorage/NSLayoutManager/NSTextContainer/NSTextView) + the line-number ruler? Add temporary `deinit` logging to `WorkspaceModel` and the two coordinators, open/close N windows, confirm dealloc and flat "persistent" bytes — growth here is the real bug. (2) **Retain cycles** around `WindowCloseGuard`'s NSWindowDelegate proxy (`retainedProxies` NSMapTable weak→strong; confirm `windowWillClose` uninstall actually runs) and the debounce/background closures in `CodeEditorView`/`MarkdownPreviewView` (both use `[weak self, weak textView]` — verify). (3) **Churn** (likely explains the 80↔190 swing, not a true leak): the `@SceneStorage` save path runs `snapshotJSON()` (a full JSONEncoder) on EVERY `ContentView` body eval — dedupe to only-on-actual-change; the per-keystroke full-document re-highlight and off-main re-render allocate transient attribute runs. (4) `FileNode` tree size for large opened folders (one node per file/dir). Expectation to validate/communicate: a large share of the baseline is irreducible SwiftUI/AppKit/TextKit/CoreAnimation framework memory that can't be cut without the rejected offload tradeoffs — so the likely deliverable is "confirm no window/buffer leak + trim the snapshotJSON churn," or a concrete leak fix. Touches (potentially) `App/WindowCloseGuard.swift`, `Editor/CodeEditorView.swift`, `Preview/MarkdownPreviewView.swift`, `Views/ContentView.swift`, `Models/WorkspaceModel.swift`. Investigation first — no code change without a confirmed cause.
- [ ] (sidebar-row-density) Tighten sidebar file-row spacing — the folder-tree rows have roughly a full extra line of vertical gap between files (see screenshot), making the sidebar feel sparse. Cause is SwiftUI `List`'s default row height/insets in `Views/SidebarView.swift` (the `List` + `OutlineGroup` at ~L43–48; rows are `FileRow`). Reduce density: set `.environment(\.defaultMinListRowHeight, …)` (~18–20 pt) on the `List` and/or apply tight `.listRowInsets(EdgeInsets(top: 0, leading: …, bottom: 0, trailing: …))`, and trim any vertical padding/frame in `FileRow` (the file icon is already 16×16). Keep the disclosure triangles, tree indentation, and selection highlight intact, and verify rows stay clickable. Depends on (folder-sidebar). Spec §5.
