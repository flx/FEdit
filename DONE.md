# DONE

Shipped items, moved here from TODO.md by `/done`.

## Features

- [x] (xcode-scaffold) Xcode project scaffold and app shell — Create `FEdit.xcodeproj` (file-system-synchronized group, macOS 26 target, Swift 5 mode, no sandbox/entitlements, ad-hoc signing, GENERATE_INFOPLIST_FILE) plus `FEdit/App/FEditApp.swift` (WindowGroup, 1100×700 default / 700×400 min, light-only appearance, GPL header boilerplate for all future sources) and a placeholder `FEdit/Views/ContentView.swift`. Accept: `xcodebuild` succeeds; app launches to an empty light window; Cmd+N opens more windows. Spec §2–§3. (shipped 2026-07-17)
- [x] (split-layout) Three-column layout with persisted draggable dividers — `Views/ContentView.swift`, new `Views/SplitDivider.swift`; `SettingsKey` constants in `App/FEditApp.swift`. Sidebar width via divider 1 (default 1/3 of default window width, clamp 160–600 pt); editor/preview split via divider 2 (default 1/2 of non-sidebar width → 1/3·1/3·1/3, clamp 15–85 %); preview column driven by a stub `isMarkdown` flag until (open-save); both positions in @AppStorage, shared across windows; sidebar width unchanged when preview toggles; resizeLeftRight hover cursor. Accept: drag both dividers, relaunch restores them. Spec §4. (shipped 2026-07-17)
- [x] (folder-sidebar) Folder sidebar with tree view — New `Models/FileNode.swift` (recursive scanner: skips dotfiles, `node_modules`, `.build`, `DerivedData`; folders-first sort), new `Models/WorkspaceModel.swift` (per-window ObservableObject: `roots`, add/remove/refresh, NSOpenPanel), new `Views/SidebarView.swift` (sections per root with `~`-abbreviated header, disclosure tree, file rows selectable with highlight, header context menu Remove/Refresh, empty-state placeholder with Open Folder button), File → Open Folder… (Cmd+Shift+O) via focusedSceneObject in `App/FEditApp.swift`. Selection just records the URL until (open-save). Depends on (split-layout). Spec §5.1–§5.3, §10. (shipped 2026-07-17)
- [x] (filter-query) Filter query language and flat filtered mode — New `Models/FilterQuery.swift` (tokenizer + parser: whitespace-separated terms, explicit uppercase AND/OR, AND binds tighter, adjacency = OR, case-insensitive substring match on the root-relative path, graceful degradation on malformed input) with unit-test-style verification; `Views/SidebarView.swift` gains the search field (rounded style) and, while non-empty, swaps each section to a flat relative-path list with "No matches" fallback. Depends on (folder-sidebar). Accept: `.py .swift` = union, `.py AND .swift` = empty, `.swift AND main OR .md` groups as (AND) OR. Spec §5.4–§5.5. (shipped 2026-07-17)
- [x] (editor-core) Editor: NSTextView wrapper with line numbers — New `Editor/CodeEditorView.swift` (NSViewRepresentable around an explicit TextKit 1 stack: wrapping always on, no horizontal scroll, monospaced 13 pt, all smart substitutions off, undo reset on file switch, binding-based text flow guarded against feedback loops) and new `Editor/LineNumberRulerView.swift` (logical-line numbers, number only on first fragment of a wrapped line, digit-adaptive gutter, visible-range-only drawing); wire into ContentView's middle column with a "No file open" placeholder; editor reports first visible line + cursor via callbacks (consumed later by (markdown-preview) and (session-restore)). Loads the sidebar-selected file read-only-ish (editing allowed, saving comes in (open-save)). Depends on (folder-sidebar). Spec §6.1–§6.2, §6.4. (shipped 2026-07-17)
- [x] (open-save) Open/save/dirty/autosave flow — `Models/WorkspaceModel.swift`: openFile state (URL, text, dirty, language stub), UTF-8-with-Latin-1-fallback read, NUL-byte binary refusal alert (plus non-regular-file and 100 MB size guards), atomic UTF-8 save, requestOpen with unsaved-changes NSAlert (Save / Always Autosave / Don't Save / Cancel; cancel reverts sidebar selection), autosave honored silently, failed autosave aborts the switch; same flow on window close/quit via an `NSWindowDelegate` forwarding proxy (`App/WindowCloseGuard.swift`). File menu in `App/FEditApp.swift`: Save (Cmd+S, disabled when clean/none) and "Autosave on File Switch" checkmark toggle (global @AppStorage, default off). Window title = file name, subtitle shows Edited marker. Real `isMarkdown` replaces the (split-layout) stub. Depends on (editor-core). Spec §7, §10. (shipped 2026-07-20)
- [x] (syntax-highlighting) Regex syntax highlighting for Swift, Python, Markdown — New `Editor/SyntaxHighlighter.swift` (language detection by extension; rule tables per SPEC token classes; application order: numbers/keywords, then strings, then comments, so strings override keywords and comments override both; markdown rules for headings/bold/italic/inline code/fences/links, with code/fence spans stripping synthesized italic) and new `Editor/Theme.swift` (light palette + fonts, also used by later preview items); wired into `Editor/CodeEditorView.swift` with ~150 ms debounce after last keystroke, full re-highlight on file switch, selection preserved. Depends on (editor-core). Accept: keyword inside a string stays string-colored; unknown extensions stay plain. Spec §6.3. (shipped 2026-07-20)
- [x] (markdown-renderer) Markdown → NSAttributedString renderer with source-line anchors — New `Preview/MarkdownRenderer.swift`: pure `String → (NSAttributedString, [MarkdownAnchor])` with a block parser (ATX headings, paragraph merging, `-`/`*`/`+` and `1.`/`1)` lists, `>` blockquotes, fenced code blocks, `---`/`***` rules) + inline parser (`**bold**`, `*italic*`, `` `code` ``, `[title](url)` with a Foundation-URL link) using a pinned nearest-closer/no-backtracking rule; emits exactly one strictly-ascending `(sourceLine → UTF-16 output location)` anchor per block, with a `\n` separator after every non-final block (including empty ones) guaranteeing strict location ascent. Consumes the shared `Theme`. Verified by a standalone `swiftc` harness (`scripts/MarkdownRendererTests/main.swift`, 133 assertions). Known limitation tracked as (md-link-scan-quadratic). Depends on (xcode-scaffold) + Theme from (syntax-highlighting). Spec §8.1–§8.2. (shipped 2026-07-20)
- [x] (markdown-preview) Preview column with editor→preview scroll sync — New `Preview/MarkdownPreviewView.swift` (read-only selectable TextKit 1 NSTextView showing `MarkdownRenderer` output; debounced re-render with pixel scroll preservation; the pure render runs off the main thread with a generation+URL staleness guard, mitigating (md-link-scan-quadratic)); `Views/ContentView.swift`: mounts the preview in the third column when the open file is markdown and feeds the editor's throttled first-visible-line into a binary-search anchor lookup (greatest anchor line ≤ editor top scrolls to top of preview), with the plan's ordering rule so edit re-renders and syncs don't fight. One-way sync only, sub-second. Depends on (markdown-renderer), (open-save), (editor-core). Spec §8.3, §4. (shipped 2026-07-20)
- [x] (editor-font-zoom) Editor font-size zoom (Cmd-+ / Cmd-− / Cmd-0), app-wide and persisted — View-menu commands (`CommandGroup(after: .toolbar)` in `App/FEditApp.swift`, `SettingsKey.editorFontSize` global `@AppStorage`, default 13, clamped 8–32) notch a global editor font size. `Editor/Theme.swift` editor fonts became size-parametric `static func(size:)` (preview `codeFont` decoupled, fixed 13, kept byte-identical so the renderer harness still compiles); `Editor/SyntaxHighlighter.swift` `highlight(_:language:fontSize:)` rebuilds attribute dicts at the size (regexes stay compiled-once); `Editor/CodeEditorView.swift` applies the size live in every window via an independent `updateNSView` block (font via re-highlight + typingAttributes — no undo/dirty pollution; `appliedFontSize` seeded in `makeNSView` so it never clobbers session-restore's cursor scroll; cursor+scroll preserved by re-pinning the first-visible line); `Editor/LineNumberRulerView.swift` gutter scales (`max(8, size−3)`). SPEC §6.1. Depends on (editor-core), (syntax-highlighting). (shipped 2026-07-20)
- [x] (column-header-bars) Column header bars for the folder and editor columns — new `Views/ColumnHeaderBar.swift` (fixed 28 pt strip, 12 pt semibold tail-truncated title, `windowBackgroundColor` fill + hairline) mounted in `Views/ContentView.swift` above the sidebar column (shows the open folder name(s): each root's `url.lastPathComponent`, comma-separated) and above the editor column (shows `openFileName`); both gated by `if` so an empty column shows no strip and no gap. Width/divider math untouched (bars are vertical-only); per-window via each window's `@StateObject workspace`. `App/FEditApp.swift` (`LayoutMetrics.columnHeaderHeight`), SPEC §4/§5.1/§5.4/§7. The existing per-root section headers and window `.navigationTitle`/`.navigationSubtitle` are complemented, not replaced. Depends on (folder-sidebar), (open-save). Spec §4. (shipped 2026-07-20)
- [x] (open-folder-new-window) Rework the folder-open menu flow — (a) renamed `File → Open Folder…` (Cmd+Shift+O) to `File → Add Folder to Window…` (unchanged focused-window add via `WorkspaceModel.addFolders`); (b) new `File → Open Folder…` (Cmd+N) opens a NEW window (value-less `WindowGroup(id: "editor")` + `openWindow(id:)`, `CommandGroup(replacing: .newItem)`) that presents a single-select directories `NSOpenPanel` on appear and opens the chosen folder as its sole root (Cancel leaves an empty window), coordinated by a `@MainActor LaunchCoordinator.pendingNewWindowPicks` mailbox drained once per pristine scene in `ContentView.onAppear`. Sidebar empty-state button relabeled to "Add Folder to Window…". The startup auto-picker was descoped after plan review found a High-severity race with session-restore; session-restore wiring left byte-for-byte unchanged. `App/FEditApp.swift`, new `App/LaunchCoordinator.swift`, `Models/WorkspaceModel.swift`, `Views/ContentView.swift`, `Views/SidebarView.swift`, SPEC.md §3/§5.1/§10. Note: adding the WindowGroup `id:` may cause a one-time session-restore reset on the first upgrade relaunch (bounded; files untouched). Depends on (folder-sidebar), (session-restore). Spec §3, §5, §10. (shipped 2026-07-20)
- [x] (session-restore) Session persistence and multi-window polish — new Foundation-only `Models/WorkspaceSnapshot.swift` (Codable, tolerant `init(from:)`; verified by `scripts/SnapshotTests/main.swift`) + `Models/WorkspaceModel.swift` snapshot/restore API (`snapshotJSON()` with `.sortedKeys` deterministic encoding, silent non-interactive open reusing `loadText`, cursor stash gated on a successful open); `Views/ContentView.swift`: `@SceneStorage` save/restore per window with a late-arrival recovery path (missing folders silently dropped, missing file not opened, cursor restored+scrolled-visible via the editor's `cursorToRestore` seam, content always re-read from disk). Frames via system restoration; menu commands already target the focused window. Depends on (open-save), (filter-query). Spec §3, §9. (shipped 2026-07-20)
- [x] (baked-in-autosave) Always-on autosave (baked in) — unconditional debounced autosave (~0.75 s after typing stops) hung off the `editorText` setter, reusing `saveOpenFile()`, plus a flush on file switch, window close/quit, and app resign-active; the on-disk file now tracks the editor within ~1 s and "dirty" is transient. Retired open-save's "Autosave on File Switch" toggle and the four-button unsaved-changes dialog (`WorkspaceModel.autosaveOnFileSwitch` + `SettingsKey.autosaveOnFileSwitch` removed); the sole surviving prompt is a minimal Close-Without-Saving/Cancel escape shown ONLY when a close/quit flush fails (so a persistently-failing save can never make the app un-quittable — Cancel is the default/Return+Escape, Close is marked destructive). The debounce re-reads `openFile`+`isDirty` at fire time (a straggler across a file switch is a no-op); exposes `cancelPendingAutosave()` and keeps `saveOpenFile()`'s success branch clean as the echo-suppression seam for (external-change-watch). `Models/WorkspaceModel.swift`, `App/FEditApp.swift`, `App/WindowCloseGuard.swift`, SPEC §7/§9/§10 (supersedes open-save criteria 9–16). Depends on (open-save). (shipped 2026-07-20)

## Bugs

- [x] (sidebar-row-density) Tighten sidebar file-row spacing — the folder-tree rows felt sparse (~a full extra line of gap). The first-pass minimal lever (`.environment(\.defaultMinListRowHeight, 20)` alone) had NO effect: `.listStyle(.sidebar)` is AppKit source-list-backed and computes its own row metrics, ignoring the floor (confirmed against a running build). Fix applied: switched `Views/SidebarView.swift`'s `List` to `.listStyle(.inset)`, which honors `defaultMinListRowHeight(20)` → tight single-line rows. Selection is custom (no `List(selection:)`) and OutlineGroup disclosure is unaffected by the style change; `FileRow` untouched. Depends on (folder-sidebar). Spec §5. (shipped 2026-07-20)
- [x] (md-link-scan-quadratic) MarkdownRenderer inline link parsing was O(n²) on bracket-heavy input — `Preview/MarkdownRenderer.swift`: each unmatched `[` re-scanned to EOF for `]`/`)`. Fixed by precomputing per-`parseNodes`-invocation nearest-`]`/`)` index arrays (`nextCloseBracket`/`nextCloseParen`, built only when the slice contains `[`), turning `parseLink`'s two to-EOF scans into O(1) lookups → true O(n), covering all three pathological families incl. `[`×N + trailing `]`. Provably a no-op memoization: byte-identical output verified by a new seeded differential fuzz in `scripts/MarkdownRendererTests/main.swift` (diffs tree + render output + anchors vs a naive reference) plus the unchanged 133 assertions. Found in markdown-renderer adversarial review. (shipped 2026-07-20)
